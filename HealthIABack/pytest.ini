[pytest]
testpaths = tests
addopts =
    --cov=.
    --cov-report=html:htmlcov
    --cov-report=xml:coverage.xml
    --cov-report=term-missing
    --junit-xml=test-report.xml

[coverage:run]
omit =
    venv/*
    tests/*
    main.py
